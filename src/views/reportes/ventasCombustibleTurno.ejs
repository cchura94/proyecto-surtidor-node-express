<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Reporte de Ventas por Turno y Combustible</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h2 class="mb-4">⛽ Ventas por Turno y Combustible</h2>

    <table class="table table-bordered table-hover">
      <thead class="table-dark">
        <tr>
          <th>Turno</th>
          <th>Empleado</th>
          <th>Producto</th>
          <th>Litros Vendidos</th>
          <th>Ingresos Generados (Bs.)</th>
        </tr>
      </thead>
      <tbody>
        <% if (ventas.length === 0) { %>
          <tr><td colspan="5" class="text-center">No hay datos disponibles</td></tr>
        <% } else { %>
          <% ventas.forEach(v => { %>
            <tr>
              <td><%= v.turno %></td>
              <td><%= v.empleado %></td>
              <td><%= v.producto %></td>
              <td><%= v.litros_vendidos %></td>
              <td><%= v.total_generado.toFixed(2) %></td>
            </tr>
          <% }) %>
        <% } %>
      </tbody>
    </table>

    <a href="/reportes" class="btn btn-secondary mt-3">⬅ Volver</a>
  </div>
</body>
</html>
